import { Brand } from "../Brand/Brand.model";
// import { Cart, CartDevice } from "../Cart/Cart.model";
// import { Device, DeviceInfo, Rating } from "../Device/Device.model";
// import { Type, TypeBrand } from "../Type/Type.model";
// import { User } from "../User/User.model";

// User.hasOne(Cart)
// Cart.belongsTo(User)

// User.hasMany(Rating)
// Rating.belongsTo(User)

// Cart.hasMany(CartDevice, { as: 'devices' })
// CartDevice.belongsTo(Cart)

// Type.hasMany(Device)
// Device.belongsTo(Type)

// Brand.hasMany(Device)
// Device.belongsTo(Brand)

// Device.hasMany(Rating)
// Rating.belongsTo(Device)

// Device.hasMany(CartDevice)
// CartDevice.belongsTo(Device)

// Device.hasMany(DeviceInfo, { as: 'info' })
// DeviceInfo.belongsTo(Device)

// Type.belongsToMany(Brand, { through: TypeBrand })
// Brand.belongsToMany(Type, { through: TypeBrand })